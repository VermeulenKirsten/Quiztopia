@model Quiztopia.Web.ViewModels.QuizQuestionVM

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Question</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Question.QuestionString)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Question.QuestionString)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        @Html.AntiForgeryToken()
        @Html.HiddenFor(vm => vm.Question.Id)
        @Html.HiddenFor(vm => vm.Question.QuestionString)
        @Html.HiddenFor(vm => vm.Question.QuestionsAnswers)
        @Html.HiddenFor(vm => vm.Question.QuizzesQuestions)
        @Html.HiddenFor(vm => vm.QuizId)
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
